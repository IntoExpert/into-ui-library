import { CircularSpinner } from "./circularSpinner";

export const CircularSpinnerArea = () => {

    return <div className={`w-full h-full p-10 flex justify-center items-center`}>
        <CircularSpinner />
    </div>;
}